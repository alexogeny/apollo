/**
 * Atom-level UI components are exported from this module. The groundwork commit
 * only prepares the surface so consuming code can begin wiring imports without
 * depending on unfinished implementations.
 */
export {};
