/**
 * Higher-order organisms such as toast viewports and modal stacks will be
 * exported from this module in future iterations. The groundwork keeps the
 * import path stable while implementation details come together.
 */
export {};
